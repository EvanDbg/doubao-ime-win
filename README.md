# Doubao Voice Input (豆包语音输入)

Windows 语音输入工具，基于豆包 ASR 实现实时语音识别。

## 功能特性

- 🎤 **实时语音识别** - 基于豆包 ASR 的高精度语音识别
- ⌨️ **快捷键触发** - 支持组合键 (Ctrl+Shift+V) 或双击模式 (双击 Ctrl)
- 📍 **悬浮按钮** - 可拖动的悬浮按钮，点击开始/停止录音
- 🔄 **流式识别** - 实时显示识别结果，支持文本修正
- 📦 **绿色便携** - 单文件可执行，无需安装

## 快速开始

### 下载使用

1. 从 [Releases](https://github.com/yourusername/doubao-ime-win/releases) 下载最新版本
2. 解压到任意目录
3. 运行 `doubao-voice-input.exe`
4. 首次运行会自动注册设备

### 使用方法

1. **快捷键模式** (默认):
   - 按下 `Ctrl+Shift+V` 开始语音输入
   - 再次按下停止录音，文本自动插入到当前焦点窗口

2. **双击模式**:
   - 在 `config.toml` 中设置 `mode = "double_tap"`
   - 快速双击 `Ctrl` 键开始/停止语音输入

3. **悬浮按钮**:
   - 点击悬浮按钮开始/停止录音
   - 拖动可调整位置

4. **系统托盘**:
   - 右键托盘图标可打开菜单
   - 选择"设置"配置热键等选项

## 配置文件

配置文件 `config.toml` 与程序同目录：

```toml
[general]
auto_start = false
language = "zh-CN"

[hotkey]
mode = "combo"  # "combo" 或 "double_tap"
combo_key = "Ctrl+Shift+V"
double_tap_key = "Ctrl"
double_tap_interval = 300

[floating_button]
enabled = true
position_x = 100
position_y = 100

[asr]
vad_enabled = true
```

## 从源码构建

### 环境要求

- Rust 1.70+ (stable)
- Windows 10/11 x64
- Visual Studio Build Tools 2022

### 构建步骤

```powershell
# 克隆项目
git clone https://github.com/yourusername/doubao-ime-win.git
cd doubao-ime-win

# 安装依赖并构建
cargo build --release

# 或使用打包脚本
.\scripts\build-portable.ps1
```

## 技术架构

- **语言**: Rust
- **语音识别**: 豆包 ASR (doubaoime-asr 协议)
- **音频采集**: cpal
- **音频编码**: Opus
- **热键**: global-hotkey
- **系统托盘**: tray-icon
- **文本输入**: Windows SendInput API

## 免责声明

> ⚠️ **注意**
> 
> 本项目基于豆包输入法客户端协议分析实现，非官方 API。
> - 仅供学习研究使用
> - 协议可能随时变更导致功能失效
> - 请遵守相关法律法规

## 许可证

MIT License

## 致谢

- [doubaoime-asr](https://github.com/starccy/doubaoime-asr) - 豆包 ASR 协议参考实现
